# Streamware - Docker Requirements (Minimal)
# Optimized for fast container startup

# Core (pinned for reproducibility)
numpy==1.26.4
pyyaml==6.0.1
python-dotenv==1.0.1

# Audio - STT/TTS (lazy-loaded)
sounddevice==0.4.6
soundfile==0.12.1
webrtcvad==2.0.10

# LLM client
httpx==0.27.0
ollama==0.3.3

# Docker & MQTT
docker==7.1.0
paho-mqtt==2.1.0

# Async
aiofiles==23.2.1
aiohttp==3.9.5

# Note: Heavy deps loaded on-demand
# - faster-whisper (STT) - optional, loaded if audio enabled
# - ultralytics (YOLO) - optional, loaded if vision enabled
# - piper-tts - optional, loaded if TTS enabled
